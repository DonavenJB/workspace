# Copy to .env. Don't commit .env.

# Server
PORT=3000
NODE_ENV=development

# Data (matches docker-compose)
MONGO_URI=mongodb://localhost:27017/empiryu
REDIS_URL=redis://localhost:6379

# Auth (Phase 2)
SESSION_SECRET=change-me-in-production
