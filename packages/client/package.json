{
  "name": "@empiryu/client",
  "version": "0.0.0",
  "private": true,
  "description": "Empiryu web client — Vite + TypeScript, PixiJS (WebGL) map to come.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@empiryu/shared": "*",
    "pixi.js": "^8.5.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
